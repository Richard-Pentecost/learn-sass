This repository has .scss files created when going through the learn-sass npm module.
To look at the exercises run: 
    npm install -g learn-sass
To access learn-sass, in the command line run:
    learn-sass